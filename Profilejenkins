pipeline {
    agent {
        docker {
            image 'maven:3.8.1-adoptopenjdk-11'
            args '--user root -v /var/run/docker.sock:/var/run/docker.sock' // mount Docker socket to access the host's Docker daemon
        }
    }
    environment {
        SONAR_URL = 'http://3.209.59.238:9000'
        DOCKER_IMAGE = "suba99ee/profilejenkins:${BUILD_NUMBER}"
    }
    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out code...'
                checkout scm
            }
        }
        stage('Build Web Application') {
            steps {
                echo 'Building web application...'
                sh 'mvn clean package' // Use 'clean package' to build a JAR file
            }
        }
        stage('Build and Push Docker Image') {
            steps {
                script {
                    def dockerfile = "/var/lib/jenkins/workspace/SubashreeProfile/Dockerfile"
                    def dockerImageTag = "suba99ee/dokimg:02"

                    // Build the Docker image
                    sh "docker build -f ${dockerfile} -t ${dockerImageTag} /var/lib/jenkins/workspace/SubashreeProfile"

                    // Push the Docker image
                    def dockerImage = docker.image("${dockerImageTag}")
                    docker.withRegistry('https://index.docker.io/v1/', credentialsId: 'dokcred') {
                        dockerImage.push()
                    }
                }
            }
        }
    }
}
